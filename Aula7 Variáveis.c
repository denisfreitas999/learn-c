#include <stdio.h>

int main(){

/*
    1) Toda variável possui um nome
    2) Toda variável possui um tipo
    3) Toda variável possui um tamanho
    4) Toda variável possui um valor

*/
    // Formas de declaração !
    // Declaração única: É declarado apenas uma variável.
    int num = 1;
   // Declaração Múltipla: É declarado mais de uma variável.
    int num1, num2 = 3;
    //Variáveis com nomes compostos devem ser iniciadas com letras minúsculas e posteriomente o segundo nome com letras maiúsculas
    double numeroVerificador;
    //Um nome de uma variável em C não pode ser iniciado por números, não pode conter espaços no nome, mas pode conter o underline _
    int nume_ros;
    int _numeros;

    // Tipos de variáveis:
    /*
        (1) char - 1 Byte ou seja, 8 bits
        (2) int - 2 Bytes, - 32768  e + 32767
        (3) float - 4 Bytes
        (4) double - 8 Bytes
        (5) void - Vazio

    */
    return 0;
}