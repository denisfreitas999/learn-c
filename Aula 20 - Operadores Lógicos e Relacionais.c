#include <stdio.h>
#include <stdlib.h>

int main () {

    // operadores lógicos
    // && - conectivo de ligação 
    // (true)&&(true)

    // ! operador NOT

    // || - operador condicional "OU"
    // (true)||(true) - true
    // (true)||(false) - true
    // (false)||(true) - true
    // (false)||(false) - false

    // função para limpar a tela do doss (clear)  => system("cls");

    return 0;

}